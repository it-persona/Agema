{% extends '@PanchAgemaAdmin/base.html.twig' %}

{% block body %}
    {% block container %}

    <div class="row">
        <div class="col-lg-12">
            <h3 class="page-header">{{ page_title }}</h3>

            <table class="table table-striped">
                <thead>
                <tr>
                    <th>#</th>
                    <th>Thumbnail</th>
                    <th>Title</th>
                    <th>Description</th>
                    <th>Category</th>
                    <th>Slug</th>
                    <th>Options</th>
                </tr>
                </thead>

                {% for product in products %}
                    <tr>
                        <td>{{ product.id }}</td>
                        <td>
                            <img src="{{ product.thumbnail.path }}" {{ product.thumbnail.height > 30 ? 'height="32"':''}} />
                        </td>
                        <td><a href="{{ path('panch_agema_agema_product_get', {'slug': product.slug}) }}">{{ product.title }}</a></td>
                        <td>{{ product.shortDescription }}</td>
                        <td>{{ product.category }}</td>
                        <td>{{ product.slug }}</td>
                        <td align="center">
                            <div class="btn-group">
                                <button type="button" class="btn btn-primary btn-sm dropdown-toggle" data-toggle="dropdown">
                                    <span class="caret"></span>
                                </button>
                                <ul class="dropdown-menu pull-right" role="menu">
                                    <li>
                                        <a href="{{ path('panch_agema_agema_product_get', {'slug': product.slug}) }}"><span class="glyphicon glyphicon-eye-open"></span> Show</a>
                                    </li>
                                    <li>
                                        <a href="#"><span class="glyphicon glyphicon-edit"></span> Edit</a>
                                    </li>
                                    <li>
                                        <a href="#"><span class="glyphicon glyphicon-trash"></span> Delete</a>
                                    </li>
                                </ul>
                            </div>
                        </td>
                    </tr>
                {% endfor %}
            </table>
        </div>
        <!-- /.col-lg-12 -->
    </div>
    <!-- /.row -->

    {% endblock container %}
{% endblock body %}